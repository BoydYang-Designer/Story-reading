# Quiz & Scores Dashboard 測試報告

**測試日期：** 2026-03-31  
**測試範圍：** quiz.js、scores-dashboard.js、index.html  
**測試方式：** 靜態程式碼分析（全文掃描）

---

## 📋 最近修改確認（本次四項需求）

| 項目 | 狀態 | 說明 |
|------|------|------|
| ① 移除所有 Mix 按鈕 | ✅ 完成 | HTML 中 Mix 按鈕全數移除（原 10 個，現 0 個） |
| ② Voice Reorder 預設 Strict 模式 | ✅ 完成 | `_vrStrictMode = true`，開始時自動同步按鈕 UI |
| ③ Play Sentence 隱藏 / 位置對調 | ✅ 完成 | 按鈕隱藏，Word bank 在上、Tap mic 在下，標籤文字也已更新 |
| ④ 音檔狀態提示（Status Bar） | ✅ 完成 | 三種狀態（⏳載入/🔊播放/▶待機）都有對應顯示與顏色 |

---

## 🔴 嚴重 Bug（功能失效）

### BUG-1：按「再試一次」在 Voice Reorder 結果頁無反應

**在哪裡：** `quiz.js` → `quiz-retry-btn` 的事件處理器

**問題說明：**  
結果頁有兩顆按鈕「再試一次」和「只練錯的」。這兩個按鈕內部的判斷邏輯（`if mode === ...`）沒有加上 `voice-reorder` 的分支。使用者做完 Voice Reorder 後，按「再試一次」什麼都不會發生，畫面卡在結果頁。

**影響：** 每次 Voice Reorder 結束後都無法重新練習，只能靠後退鍵回選單重新選。

**修法：**
```js
// quiz-retry-btn 裡加：
else if (mode === 'voice-reorder') startVoiceReorder(subpanelSource['voice-reorder'] || 'note');

// quiz-retry-wrong-btn 裡加：
else if (mode === 'voice-reorder') _vrStartRetryWrong();  // 需另外實作
```

---

### BUG-2：「只練錯的」在 Voice Reorder 同樣無反應

**在哪裡：** `quiz.js` → `quiz-retry-wrong-btn` 的事件處理器

**問題說明：** 同 BUG-1，只是另一顆按鈕。目前 Voice Reorder 也沒有 RetryWrong 的對應函式，需要一起新增。

**影響：** 無法針對答錯的句子重新練習。

---

## 🟡 中等 Bug（功能有瑕疵但不會卡住）

### BUG-3：Voice Reorder 出題不使用間隔重複算法

**在哪裡：** `quiz.js` → `startVoiceReorder()` 的抽題邏輯

**問題說明：**  
其他所有模式（Flashcard、Dictation、Reorder）都用 `weightedSample()` 來抽題，優先給出陌生或已遺忘的句子。Voice Reorder 卻只用 `shuffle().slice()`，完全隨機，不管這句話你是否已經很熟了。

**影響：** 練了很多次之後，每次仍可能重複看到熟悉的句子，浪費練習時間。

**修法：** 把 `shuffle(sentences).slice(0, n)` 改成呼叫 `weightedSample(...)`（同 Reorder 模式）。

---

### BUG-4：口說練習成績不計入熟悉度分數

**在哪裡：** `scores-dashboard.js` → `calcWeightedFamiliarity()`

**問題說明：**  
口說成績（voiceReorder source）雖然有正確記錄到 `itemScores`，但計算熟悉度時完全沒被納入。例如 noteSentences 的計算只看 `reorder 70% + dictation 30%`，voiceReorder 的成績完全被忽略。

**影響：** 就算口說練了很多遍也答對，Scores Dashboard 顯示的熟悉度不會提升。

**修法（可選）：** 在 noteSentences/articleSentences 的加權公式中加入 voiceReorder 作為輔助分，例如 `reorder 60% + voiceReorder 20% + dictation 20%`（權重可自行調整）。

---

## 🔵 小問題（視覺/體驗瑕疵）

### BUG-5：答對後的綠色閃爍殘留到下一題

**在哪裡：** `quiz.js` → `_vrLoadQuestion()`

**問題說明：**  
答對時，答案區會加上 `vr-correct-flash` class 顯示綠色閃爍效果。但 `_vrLoadQuestion()` 在載入下一題時沒有清除這個 class，所以下一題一開始答案區還是綠色的。

**影響：** 視覺上會誤以為還在上一題的結果，稍微混淆。

**修法：** 在 `_vrLoadQuestion()` 的「Reset UI」區段加一行：
```js
_vrEl('vr-answer-zone').classList.remove('vr-correct-flash');
```

---

### BUG-6：進度條百分比和顯示數字不同步

**在哪裡：** `quiz.js` → `_vrUpdateProgress()`

**問題說明：**  
進度條的百分比用 `done / total * 100`（其中 `done = qIndex`），但上方文字顯示 `done + 1 / total`。第一題時文字顯示「1 / 10」但進度條是 0%，感覺沒在動。

**影響：** 輕微的視覺不一致，第一題進度條看起來是空的。

**修法：** 把百分比改成 `(done + 1) / total * 100`，讓進度條和文字一致。

---

### BUG-7：TTS 模式（無音檔時）Status Bar 不顯示 loading 狀態

**在哪裡：** `quiz.js` → `_vrPlaySentence()` 的 TTS fallback 分支

**問題說明：**  
有音檔時（hasAudio = true），播放前會呼叫 `onLoading` 讓 status bar 顯示「⏳ Loading audio…」。但走 TTS 語音合成的路徑時，直接跳到 `playing` 狀態，沒有 `loading` 過渡。雖然 TTS 不需要下載，但邏輯不一致。

**影響：** 小問題，TTS 本身反應很快所以幾乎感覺不到。

---

## ✅ 確認正常的功能

- **所有 Quiz 模式的音檔播放**（Flashcard、Dictation、Reorder、Article）邏輯正確
- **間隔重複算法**（weightedSample、calcEffectiveFamiliarity、時間衰減）邏輯正確
- **Scores Dashboard 分類瀏覽、排序、細節頁**邏輯正確
- **清除紀錄、整理孤立紀錄、匯出/匯入**功能邏輯正確
- **Voice Reorder 語音辨識、Strict/Fuzzy 模式、拖曳/點擊放字**邏輯正確
- **Firestore 同步**（上傳、下載）在有登入時會正確執行
- **難度篩選**（selectedDifficulties）在 VR 模式有正確套用
- **答對/答錯 recordItemResult 記錄**在 VR 模式有正確執行（已有 Bug 2 Fix）

---

## 📌 建議優先修復順序

| 優先 | Bug | 原因 |
|------|-----|------|
| 1 | BUG-1, BUG-2（再試一次/只練錯的） | 功能完全失效，使用者每次做完 VR 都無法重試 |
| 2 | BUG-5（綠色殘留） | 修改只需一行，CP 值高 |
| 3 | BUG-3（出題算法） | 長期使用才感受到，但影響學習效率 |
| 4 | BUG-4（口說不計分） | 設計決策，可視口說的重要性決定是否納入 |
| 5 | BUG-6（進度條） | 視覺小問題 |
| 6 | BUG-7（TTS loading） | 幾乎感覺不到 |

---

*報告產出方式：靜態程式碼分析，未執行真實瀏覽器測試。建議對 BUG-1/2 在實際裝置上驗證確認。*
